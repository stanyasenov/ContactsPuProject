<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.puprojects.stanimir.ContactsProject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="ContactsPU_Project"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.puprojects.stanimir.ContactsProject.UpdateActivity"
            android:parentActivityName="com.puprojects.stanimir.ContactsProject.MainActivity"/>
        <activity
            android:name="com.puprojects.stanimir.ContactsProject.AddActivity"
            android:label="Add Contact"
            android:parentActivityName="com.puprojects.stanimir.ContactsProject.MainActivity" />
        <activity
            android:name="com.puprojects.stanimir.ContactsProject.MainActivity"
            android:label="Contacts">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>